<% layout('layouts/boilerplate')%>
    <h1 class="underline">All Items</h1>

    <div class="items-list">
        <ul>
            <% for(let item of items) { %>
                <li>
                    <%= item.title %>
                </li>
                <% } %>
        </ul>
    </div>